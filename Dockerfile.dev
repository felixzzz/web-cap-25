FROM node:20-alpine

WORKDIR /app

# Install dependencies only when package.json changes
COPY package.json package-lock.json* ./
RUN npm install

# Expose the standard Next.js port (or 3045 if preferred)
EXPOSE 3045

# Command to run in development mode
CMD ["npm", "run", "dev"]
